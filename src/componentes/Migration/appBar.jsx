import { Box, Card, CardContent, Button, ButtonGroup, Typography, Toolbar,AppBar } from "@mui/material";
const AppBarContent = ()=>{
    return (
        <AppBar>
            <Toolbar><Typography>Menu Principal</Typography></Toolbar>
        </AppBar>
    );
}
export default AppBarContent;